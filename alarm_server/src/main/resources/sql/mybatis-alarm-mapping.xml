<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="AlarmMapper">
	<select id="findLatestAlarm" resultType="alarm" parameterType="int">
		select alarmid, uid, alarmtype, boardid, commentid, follow, nickname
		from alarm
		where uid = #{value} order by alarmid desc limit 1;
	</select>
	
	<select id="getAlarm" parameterType="int" resultType="alarm">
		select alarmid, uid, alarmtype, boardid, commentid, follow, nickname
		from alarm
		where uid = #{value} order by alarmid desc
	</select>
</mapper>